import './Perfil.css'

function Button({cor, frase, contador, atualiza}){
    function click(){
       
    }
    return(
        <button > 🔍  Mais Noticias
        </button>);
        
}
export default Button;